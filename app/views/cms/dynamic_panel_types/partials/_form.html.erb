<div class="container">
  <%= simple_form_for [:cms, dynamic_panel_type] do |f| %>
    <fieldset class="form-group">
      <%= f.input :title, label: 'Title' %>
      <%= f.input :cms_name, label: 'Cms Name' %>
    </fieldset>

    <%= f.button :submit, class: 'btn btn-dark'  %>
  <% end %>
</div>
